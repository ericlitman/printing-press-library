{
  "schema_version": 1,
  "applied_at": "2026-05-15",
  "base_run_id": "20260510-215840",
  "base_printing_press_version": "4.2.2",
  "patches": [
    {
      "id": "dedupe-exit-code-braces",
      "summary": "Fix mismatched braces in report dedupe so the package compiles, and only return exit code 2 when the top row's score actually clears the 0.8 duplicate threshold.",
      "reason": "The first attempt to add the score-gated exit code left an orphan return statement and an extra closing brace between the inner if-block and the function's final return nil, producing five Go syntax errors on the build. The orphan return also made the score-threshold check redundant by exiting 2 for any non-empty result set.",
      "files": [
        "internal/cli/report_dedupe.go"
      ],
      "validated_outcome": "go build ./... passes; report_dedupe.go compiles and the score >= 0.8 gate is the sole exit-2 trigger."
    },
    {
      "id": "asset-in-scope-helper",
      "summary": "Add a wildcard-aware assetInScope(asset, scope) helper used by the guarded report-submission path and refer to it from report_submit.go.",
      "reason": "report_submit.go called an assetInScope helper that was never defined, so the package did not compile. The previous strings.Contains-based check matched only exact substrings, so a wildcard scope like *.example.com never matched a concrete asset like api.example.com.",
      "files": [
        "internal/cli/transcend_helpers.go",
        "internal/cli/report_submit.go"
      ],
      "validated_outcome": "go build ./... passes; api.example.com matches *.example.com, example.com matches *.example.com, and unrelated assets are rejected."
    },
    {
      "id": "chrome-leveldb-cross-platform",
      "summary": "Resolve the Chrome Default-profile LocalStorage LevelDB path per OS (macOS / Linux / Windows) instead of hardcoding the macOS path.",
      "reason": "The original auth_login_chrome.go path was macOS-only (~/Library/Application Support/...). Linux and Windows would return 'opening Chrome LocalStorage leveldb: no such file or directory' even with Chrome installed at the standard system location.",
      "files": [
        "internal/cli/auth_login_chrome.go"
      ],
      "validated_outcome": "chrome --chrome login resolves the correct LevelDB path on darwin, linux, and windows; falls back to a clear error message on other platforms."
    }
  ]
}
